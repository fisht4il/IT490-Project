<!DOCTYPE html>
<html lang="en">

<head>
  <link rel="stylesheet" href="style.css">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Form</title>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script>
    $(document).ready(function () {
      $('#login-form').on('submit', function (e) {
        e.preventDefault();

        var username = $('#username').val();
        var password = $('#password').val();
        var showRawJson = $('#show-json').is(':checked');

        $.ajax({
          url: 'login.php',
          type: 'POST',
          data: {
            username: username,
            password: password
          },
          success: function (response) {
            try {
              let result = typeof response === 'string' ? JSON.parse(response) : response;
              let color = result.success ? 'green' : 'red';

              let formattedResponse = `
                <pre><strong style="color:${color};">${result.message}</strong></pre><br>
              `;

              let rawJsonResponse = showRawJson
                ? `<h4>Raw JSON Response:</h4><pre>${JSON.stringify(result, null, 2)}</pre>`
                : '';

              $('#response').html(formattedResponse + rawJsonResponse);
		
	      if (result.success && result.redirect) {
                window.location.href = result.redirect;
                return;
              }

            } catch (error) {
              console.error("JSON parse error:", error);
              $('#response').html(
                `<pre><strong style='color:red;'>Invalid JSON response.</strong></pre>`
              );
            }
          },
          error: function () {
            $('#response').html("<strong style='color:red;'>An error occurred. Please try again.</strong>");
          }
        });
      });
    });
  </script>
</head>

<body>
  <header class="main-header">
        <h1 class="page-title">IT-490-Project</h1>
        <nav class="header-nav">
            <ul class="link-list">
                <li class="link-item">
                    <button class="btn">
                        <a href="#" class="placeholder-link">Link1</a>
                    </button>
                </li>
                <li class="link-item">
                    <button class="btn">
                        <a href="#" class="placeholder-link">Link2</a>
                    </button>
                </li>
                <li class="link-item">
                    <button class="signup">
                        <a href="signup.html" class="placeholder-link">Signup</a>
                    </button>
                </li>
            </ul>
        </nav>
  </header>

  <main class="main-section">
        <form action="/php/login.php"  class="login-form" id="login-form">  
            <img src="kraken-svgrepo-com.png" alt="" class="logo-image">  
            <h2 class="login-title">Login</h2>
            <div class="user-div">
              <label class="username-label" for="username">Username</label>
              <input type="text" class="input-username" id="username" name="username" required><br><br>
            </div>
            <div class="password-div">
              <label class="password-label" for="password">Password</label>
              <input type="password" class="input-password" id="password" name="password" required><br><br>
            </div>
            <input type="submit" class="input-submit" value="Login">
            <label>
                <br><br>Raw JSON Response<input type="checkbox" id="show-json">
            </label>
        </form>
  </main>
  <div id="response"></div>

  <footer class="footer">
        <p class="copyright">&copy; 2024. Copyright by IT-490-Project</p>
  </footer>
</body>
</html>


/* --ANIMATIONS-- */

@keyframes fade-in {
    0% {
        opacity: 0;
        transform: translateY(-20px);
    }

    100% {
        opacity: 1;
        transform: translateY(-0px);
    }
}


/* --GENERAL-- */

* {

}

html {

}

body { 
    background-attachment: fixed;
    background-color: rgba(0, 0, 0, 0.6);
    background-blend-mode: darken;
    background-size: cover;
    color: #0566c7;
    display: flex;
    flex-direction: column;
    height: 100vh;
    background-color: #1e135e;
    color: white;

}

/* --HEADER-- */

.main-header {
    background-color: rgba(0, 0, 0, 0.3);
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 1125px;
    margin-left: auto;
    margin-right: auto;
    animation: fade-in 0.5s;
}

.page-title {
    margin-left: 25px;
    font-size: 25px;
    font-weight: 1000;
    color: rgba(225, 225, 225, 1)
}


.header-nav {

}

.link-list {
    list-style: none;
    display: flex;
}

.link-item {
    margin-right: 25px;
}

.btn {
    background: none;
    border: none;
    width: 90px;
    height: 60px;
}

.placeholder-link {
    text-decoration: none;
    color: rgba(225, 225, 225, 0.75);
    font-weight: 500;
    font-size: 20px;
    transition: all 0.5s;
}

.placeholder-link:hover {
    color: rgba(225, 225, 225, 1);
}

.signup {
    width: 90px;
    height: 60px;
    border: none;
    background-color: #5741d9;
    border-radius: 10px;
}




/* --MAIN-- */

.main-section {
    display: flex;
    justify-content: center;
    align-items: center;

}

.login-form {
    position: relative;
    display: flex;
    flex-direction: column;
    width: 300px;
    margin-left: auto;
    margin-right: auto;
}

.user-div {
    position: relative;
    display: flex;
    flex-direction: column;
    width: 300px;
    margin-left: auto;
    margin-right: auto;
}

.password-div {
    position: relative;
    display: flex;
    flex-direction: column;
    width: 300px;
    margin-left: auto;
    margin-right: auto;
}


.login-title {
    text-align: center;
}

.logo-image {
    display: inline-flex;
    height: 55px;
    width: 55px;
    display: inline;
    padding-top: 10px;
}

.username-label {
    position: absolute;
    left: 16px;
    top: 20px;
    padding:0 8px;
    color: white;
    cursor: text;
    transition: top 200ms ease-in, left 200ms ease-in, font-size 200ms ease-in;
}

.user-div:focus-within .username-label {
    top:-6px;
    font-size: 12px;
}

.input-username {
    padding: 20px;
    margin-bottom: 10px;
    border-radius: 2px solid;
    border-radius: 10px;
    color: white;
    outline: none;
    background: none;
}

.input-username:hover {
    border-color: #5741d9
}

.input-username:focus {
    border-color: blue;
}


.password-label {
    position: absolute;
    left: 16px;
    top: 20px;
    padding: 0 8px;
    color: white;
    cursor: text;
    transition: top 200ms ease-in, left 200ms ease-in, font-size 200ms ease-in;

}

.password-div:focus-within .password-label {
    top:-6px;
    font-size: 12px;
}

.input-password {
    padding: 20px;
    margin-bottom: 10px;
    border-radius: 2px solid;
    border-radius: 10px;
    color: white;
    outline: none;
    background: none;
}

.input-password:hover {
    border-color: #5741d9
}

.input-password:focus {
    border-color: blue;
}

.input-submit {
    background-color: #5741d9;
    color: white;
    border: none;
    padding: 20px;
    cursor: pointer;
    transition: background-color 0.5s;
    width: 100%;
    border-radius: 10px;
}

.input-submit:hover {
    background-color: #a396ea;
}




/* --FOOTER-- */

.footer {
    display: flex;
    width: 1125px;
    margin-left: auto;
    margin-right: auto;
    margin-top: auto;
    border-top: 1px solid #5741d9;
    background-color: rgba(0, 0, 0, 0.3);
    padding-left: 2px;
}

.copyright {

}
